{
  "name": "backend",
  "version": "0.0.0",
  "description": "Root backend express app, were all backend middleware and routes are atached to",
  "main": "app.js",
  "private": true,
  "plugin": {
    "provides": [
      "backend"
    ],
    "consumes": ["server"]
  }
}
