angular.module('bauhaus.dashboard.controllers', []);

angular.module('bauhaus.dashboard.controllers').controller('DashboardCtrl', ['$scope', function ($scope) {
    'use strict';
  
    $scope.message = "Welcome to the BauhausJS dashboard"

}]);